# Deny all direct HTTP access to /data — only PHP scripts on the server
# should be able to read/write these files.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
